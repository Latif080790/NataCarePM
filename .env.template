P# Environment Variables Template for NataCarePM
# Copy this file to .env and fill in your actual values

# ==========================================
# PRIORITY 2C: MONITORING & ANALYTICS
# ==========================================

# Sentry Configuration (Error Tracking & Performance Monitoring)
# Get your DSN from: https://sentry.io/settings/[your-org]/projects/[your-project]/keys/
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id
VITE_SENTRY_ENABLED=false  # Set to 'true' to enable Sentry in development

# Google Analytics 4 Configuration
# Get your Measurement ID from: https://analytics.google.com/
VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX
VITE_GA4_ENABLED=false  # Set to 'true' to enable GA4 in development

# ==========================================
# PRIORITY 2A: PWA CONFIGURATION
# ==========================================

# Push Notification VAPID Public Key
# Generate with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key

# ==========================================
# APP CONFIGURATION
# ==========================================

# App Version (for release tracking)
VITE_APP_VERSION=1.0.0

# Environment
VITE_APP_ENV=development  # development | staging | production

# ==========================================
# FIREBASE CONFIGURATION
# ==========================================

# Firebase Project Configuration
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ==========================================
# PLAYWRIGHT E2E TESTING
# ==========================================

# Base URL for Playwright tests
PLAYWRIGHT_BASE_URL=http://localhost:5173

# ==========================================
# NOTES
# ==========================================

# 1. NEVER commit the actual .env file to version control
# 2. Keep this .env.template file updated with new variables
# 3. Each developer should create their own .env file from this template
# 4. Production environment variables should be set in your hosting platform
#    (e.g., Firebase Hosting, Vercel, Netlify, etc.)
# 5. For CI/CD, set these as secrets/environment variables in your pipeline

# ==========================================
# HOW TO GET CREDENTIALS
# ==========================================

# Sentry:
# 1. Sign up at https://sentry.io/
# 2. Create a new project
# 3. Copy the DSN from Project Settings → Client Keys (DSN)

# Google Analytics 4:
# 1. Go to https://analytics.google.com/
# 2. Create a GA4 property
# 3. Go to Admin → Data Streams → Web
# 4. Copy the Measurement ID (starts with G-)

# VAPID Keys for Push Notifications:
# 1. Install web-push: npm install -g web-push
# 2. Generate keys: npx web-push generate-vapid-keys
# 3. Copy the public key to VITE_VAPID_PUBLIC_KEY
# 4. Store the private key securely (use in backend/cloud functions)
